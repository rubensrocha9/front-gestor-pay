import { DocumentPipePipe } from './document-pipe.pipe';

describe('DocumentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
